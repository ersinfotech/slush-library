{
  "name": "<%= name %>",
  "version": "0.0.0",
  "main": "lib/index.js",
  "scripts": {
    "clean": "rimraf lib",
    "build": "babel src --out-dir lib",
    "prepublish": "npm run clean && npm run build"
  },
  "devDependencies": {
    "babel": "^5.8.23",
    "rimraf": "^2.4.3"
  }
}
